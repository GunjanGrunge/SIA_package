---
name: sia
description: Continue SIA only when the user explicitly requests the SIA workflow.
---

# SIA (Codex adapter source)

Run `sia next --json` and follow the persisted stage. Read `sia/AGENT.md` if
vendored. Record every native subagent dispatch and independent review through
the CLI. Coexist with other skills and never replace project `AGENTS.md`.
