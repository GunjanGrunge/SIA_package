---
inclusion: manual
---

# SIA (Kiro adapter source)

Run `sia next --json` and follow the persisted stage. In orchestrator mode use
Kiro's native subagents, recording dispatch and review evidence through the
CLI. Leave other steering, hooks, skills, and agent files unchanged.
