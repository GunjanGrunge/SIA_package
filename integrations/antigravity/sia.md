---
description: Continue the persistent SIA workflow for this project
---

# SIA (Antigravity workflow source)

On `/sia`, run `sia next --json` and follow the persisted stage. Use native
agents for implementation and review, record their IDs through the CLI, and
leave all other `.agents/` content unchanged.
